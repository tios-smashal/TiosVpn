#!/bin/bash
#
# TiosVPN - Main application launcher
#

# Get the directory where the app bundle is located
APP_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")/../Resources" && pwd)"

# Run the GUI script
exec "$APP_DIR/TiosVPN-GUI.sh"
